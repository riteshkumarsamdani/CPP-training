This code is for printing the pattern on the basis of user input.
We will take input and print pattern as total (input * 2 + 1) rows in pattern.
To compile code we will run command in terminal as: g++ pattern_assignment.cpp -o pattern_assignment.
it will compile code and make executable file named pattern_assignment.
to run code we will give command as: ./pattern_assignment
now we have to give value to print pattern on screen.